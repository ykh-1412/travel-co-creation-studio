services:
  travel-co-creation:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
